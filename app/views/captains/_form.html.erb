<%= form_with(model: captain, local: true) do |form| %>
  <% if captain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(captain.errors.count, "error") %> prohibited this captain from being saved:</h2>

      <ul>
      <% captain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :captain_user_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :captain_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
